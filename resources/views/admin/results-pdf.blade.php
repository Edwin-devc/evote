<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Election Results</title>
    <style>
        body {
            font-family: DejaVu Sans, Arial, sans-serif;
            font-size: 12px;
            color: #111111;
        }
        .header {
            text-align: center;
            margin-bottom: 16px;
        }
        .header h1 {
            margin: 0 0 4px 0;
            font-size: 18px;
        }
        .meta table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 16px;
        }
        .meta td {
            padding: 4px 6px;
        }
        .section {
            margin-top: 18px;
        }
        .position-title {
            font-size: 14px;
            font-weight: bold;
            margin-bottom: 6px;
        }
        table.results {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 6px;
        }
        table.results th,
        table.results td {
            border: 1px solid #cccccc;
            padding: 6px;
        }
        table.results th {
            background: #f2f2f2;
            text-align: left;
        }
        .num {
            text-align: right;
        }
        .winner {
            background: #e9f5e9;
            font-weight: bold;
        }
        .note {
            font-size: 10px;
            color: #555555;
            margin-top: 8px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Election Results</h1>
        <div>{{ config('app.name') }}</div>
    </div>

    <div class="meta">
        <table>
            <tr>
                <td>Total voters</td>
                <td class="num">{{ number_format($totalVoters) }}</td>
                <td>Participated</td>
                <td class="num">{{ number_format($participation) }}</td>
                <td>Turnout</td>
                <td class="num">{{ number_format($turnout, 2) }}%</td>
            </tr>
            <tr>
                <td>Generated at</td>
                <td>{{ $generatedAt->format('Y-m-d') }}</td>
                <td>Generated by</td>
                <td colspan="3">{{ auth()->user()?->name ?? 'System' }}</td>
            </tr>
        </table>
    </div>

    @forelse ($positions as $position)
        @php
            $totalPositionVotes = $position->candidates->sum('num_votes');
            $winnerVotes = $position->candidates->max('num_votes');
        @endphp
        <div class="section">
            <div class="position-title">{{ $position->name }}</div>

            @if ($position->candidates->isEmpty())
                <div>No candidates for this position.</div>
            @else
                <table class="results">
                    <thead>
                        <tr>
                            <th>Candidate</th>
                            <th class="num">Votes</th>
                            <th class="num">Percent</th>
                            <th>Result</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach ($position->candidates as $candidate)
                            @php
                                $percent = $totalPositionVotes > 0
                                    ? number_format(($candidate->num_votes / $totalPositionVotes) * 100, 2)
                                    : '0.00';
                                $isWinner = $winnerVotes > 0 && $candidate->num_votes === $winnerVotes;
                            @endphp
                            <tr class="{{ $isWinner ? 'winner' : '' }}">
                                <td>{{ $candidate->name }}</td>
                                <td class="num">{{ number_format($candidate->num_votes) }}</td>
                                <td class="num">{{ $percent }}%</td>
                                <td>{{ $isWinner ? 'Winner' : '' }}</td>
                            </tr>
                        @endforeach
                    </tbody>
                </table>
            @endif
        </div>
    @empty
        <p>No positions found.</p>
    @endforelse
</body>
</html>
